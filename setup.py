from setuptools import setup, find_packages

setup(name='pointcloud_invariance_smoothing',
      version='0.1.0',
      description='invariance-aware smoothing for pointclouds',
      packages=find_packages(),
      zip_safe=False)
